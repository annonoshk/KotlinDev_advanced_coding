package db.exposed.advancedExposed

/**
 * @description : DB setup for Exposed
 *
 * Setting Up a Kotlin Project with Exposed
To use Exposed in a Kotlin project, you'll need to set up the Kotlin environment and include the Exposed library.

Here's a step-by-step guide:

Create a Kotlin Project:
If you're using IntelliJ IDEA, you can create a new project and select Kotlin as the language.

Add Dependencies:
Include the Exposed library in your build.gradle.kts file:

Configure the Database Connection: You need to configure the database connection using the Database.connect method.
You can choose the appropriate driver and connection string for your database:
 */